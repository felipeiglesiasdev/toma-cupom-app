<header class="header">
    <div>
        <!-- LOGO -->
        <a href="{{ url('/') }}">
            Toma Cupom
        </a>
        <!-- MENU -->
        <nav>
            <a href="{{ url('/') }}">Início</a>
            <a href="{{ url('/lojas') }}">Lojas</a>
            <a href="{{ url('/categorias') }}">Categorias</a>
            <a href="{{ url('/contato') }}">Contato</a>
        </nav>
    </div>
</header>